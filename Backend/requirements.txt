# Runtime dependencies only. Generated from pyproject.toml's [project]
# dependencies — see that file for why each one is here.
#
# A-07: this file used to be a raw `pip freeze`. That mixed runtime with dev
# tooling (mypy shipped to production), pinned transitive packages as if they
# were direct choices, and listed BOTH psycopg2-binary and psycopg v3 — so the
# driver the app used depended on the connection string rather than a decision.
#
# Install for development instead with:   pip install -e ".[dev]"
# Install for a container with:            pip install -r requirements.txt

fastapi>=0.123,<0.124
uvicorn[standard]>=0.38,<0.39

pydantic>=2.12,<3
pydantic-settings>=2.12,<3
email-validator>=2.3

sqlalchemy>=2.0.44,<2.1
alembic>=1.17,<2
psycopg[binary]>=3.3,<4

PyJWT>=2.10,<3
passlib>=1.7.4
argon2-cffi>=25.1

python-multipart>=0.0.20
python-dotenv>=1.2
typer>=0.20,<1
tzdata>=2025.2
